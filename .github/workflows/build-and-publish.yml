name: Build and Publish to CDN

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: write
  pull-requests: write
  issues: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "23"

      - name: Install dependencies
        run: npm install

      - name: Build project
        run: npm run build:cdn

      - name: Create build artifacts
        run: |
          mkdir -p build-artifacts
          cp -r dist build-artifacts/
          cp -r temp build-artifacts/
          cp -r cdn-dist build-artifacts/
          cp package.json build-artifacts/
          cp README.md build-artifacts/

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-files
          path: build-artifacts/
          retention-days: 30

  release:
    needs: build
    name: Release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "23"

      - name: Install dependencies
        run: npm i

      - name: Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npx semantic-release

  create-cdn-dist:
    needs: release
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push' && !contains(github.event.head_commit.message, 'Add CDN distribution files for build')

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: true

      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          name: build-files
          path: build-artifacts/

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "23"

      - name: Use CDN-ready files
        run: |
          # The CDN files are already created by the build:cdn script
          echo "Using pre-built CDN files from cdn-dist/"
          ls -la build-artifacts/cdn-dist/

      - name: Commit CDN files to repository
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Copy CDN files to the repository root
          cp -r build-artifacts/cdn-dist/ ./

          # Configure git
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"

          # Add and commit CDN files
          git add cdn-dist/
          git commit -m "Add CDN distribution files for build ${{ github.run_number }}" || echo "No changes to commit"

          # Push to repository (handle remote changes by fetching and rebasing)
          git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git
          git fetch origin main
          git rebase origin/main || git pull --rebase origin main || true
          git push origin HEAD:main

      - name: Get Release Info
        id: release_info
        uses: actions/github-script@v6
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const release = await github.rest.repos.getLatestRelease({
              owner: context.repo.owner,
              repo: context.repo.repo
            });
            core.setOutput('upload_url', release.data.upload_url);
            core.setOutput('tag_name', release.data.tag_name);
            core.setOutput('name', release.data.name);

      - name: Debug Release Info & files
        run: |
          echo "release_tag=${{ steps.release_info.outputs.tag_name }}"
          echo "upload_url=${{ steps.release_info.outputs.upload_url }}"
          ls -la build-artifacts/cdn-dist || true
          du -sh build-artifacts/cdn-dist/* || true

      - name: Authenticate GitHub CLI
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "Authenticating gh CLI"
          echo "${{ secrets.GITHUB_TOKEN }}" | gh auth login --with-token

      - name: Upload release assets (gh)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "Uploading assets to release ${{ steps.release_info.outputs.tag_name }}"
          gh release upload "${{ steps.release_info.outputs.tag_name }}" build-artifacts/cdn-dist/modo-widget.js --clobber || echo "upload failed: modo-widget.js"
          gh release upload "${{ steps.release_info.outputs.tag_name }}" build-artifacts/cdn-dist/modo-web-component.js --clobber || echo "upload failed: modo-web-component.js"
          gh release upload "${{ steps.release_info.outputs.tag_name }}" build-artifacts/cdn-dist/modo-widget.min.js --clobber || echo "upload failed: modo-widget.min.js"
          gh release upload "${{ steps.release_info.outputs.tag_name }}" build-artifacts/cdn-dist/modo-web-component.min.js --clobber || echo "upload failed: modo-web-component.min.js"

      - name: Upload Dist Folder
        run: |
          cd build-artifacts/cdn-dist
          tar -czf dist.tar.gz dist/

      - name: Upload Dist Archive
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.release_info.outputs.upload_url }}
          asset_path: ./build-artifacts/cdn-dist/dist.tar.gz
          asset_name: dist.tar.gz
          asset_content_type: application/gzip
